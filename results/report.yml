#
# Collect logs and build reports after the test execution
#
---

- hosts: compute

  tasks:
  - name: Get info about test run
    shell: |
      echo "Test: {{ test_name }}" > info.txt
      echo -n "Start: " >> info.txt
      stat -c '%z' pgbench_init.log >> info.txt
      echo -n "Finish: " >> info.txt
      stat -c '%z' pgbench.log >> info.txt
    args:
      chdir: "/var/db/postgres/{{ test_name }}"
    become: yes

  - name: Extract info from pg.log
    shell: |
      cat pg.log | grep -v "sending message" | grep -v "START_REPLICATION SLOT" > short.log
      grep "sending message" pg.log | awk '{print $10}' > sizes.log
    args:
      chdir: "{{ main_dir }}/{{ test_name }}"
      creates: "{{ main_dir }}/{{ test_name }}/sizes.log"
    become: true

  - name: Download files
    synchronize:
      mode: pull
      src: /var/db/postgres/{{ test_name }}/{{ item }}
      dest: ./{{ item }}
    become: yes
    loop:
    - pgbench.log
    - pgbench_init.log
    - postgresql.conf
    - info.txt
    - short.log
    - sizes.log

  - name: Download common logs
    synchronize:
      mode: pull
      src: /var/db/postgres/{{ item }}
      dest: ./{{ item }}
    become: yes
    loop:
    - disk_test.log
    - ping_test.log

- hosts: safekeepers
  
  tasks:
  - name: Fetch disk_test.log
    synchronize:
      mode: pull
      src: "{{ main_dir }}/disk_test.log"
      dest: "./disk_test_{{ private_ip }}.log"
    become: yes
